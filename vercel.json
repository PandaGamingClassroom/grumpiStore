{
  "version": 2,
  "builds": [
    {
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/grumpi-store"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
